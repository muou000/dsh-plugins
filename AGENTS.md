# AGENTS.md

本工作区是 DeepSeek Harness（DSH）的仓库外插件集合。顶层仓库管理 submodule 版本组合、共同规范和组合验证；每个一级插件目录是独立 Git 仓库，拥有自己的源码、测试、发布与变更历史。

## 指令优先级与资料来源

1. 当前任务中的用户要求。
2. 目标插件目录内更具体的 `AGENTS.md`。
3. 本文件及 [`docs/PLUGIN_STANDARD.md`](docs/PLUGIN_STANDARD.md)、[`docs/EVALUATION.md`](docs/EVALUATION.md)。
4. 相邻 DSH 源码仓库的公开架构文档和类型定义。

开始设计前阅读 `D:\deepseek-harness\docs\architecture.md`；涉及生命周期、并发、进程或卸载时还要阅读其 `docs/defensive-patterns.md`。将 DSH 视为上游依赖：除非任务明确要求，不修改 `D:\deepseek-harness`。

## 仓库边界

- 顶层仓库只保存治理文档、组合脚本和 submodule gitlink，不复制插件源码。
- 功能变更在目标插件仓库中提交。先提交插件，再更新顶层 gitlink。
- 跨插件改动拆成各自可审查的提交，并记录兼容顺序；不要用一个仓库的提交掩盖另一个仓库的未提交修改。
- 不在未获明确要求时创建远程仓库、推送、发布、改写远程历史或批量更新所有 submodule。
- 新插件默认命名为 `dsh-<capability>`，采用独立仓库，并通过 `git submodule add` 加入顶层。

每次工作前分别检查顶层和目标插件的 `git status`。已有修改属于用户；保留无关修改，只处理任务涉及的文件。

## 架构原则

- **通过插件扩展。** 使用 DSH 已公开的 Cordis service、typed event、registry 和 effect，不修改 agent loop 来绕过扩展点。
- **注册必须可逆。** 事件监听、服务注册、定时器、文件观察器和后台任务都必须绑定插件生命周期，并在卸载时释放。
- **能力接缝完整。** 新的可替换能力要明确 Service Definition、Provider 和 Consumer；只有在角色独立演进时才拆包。
- **模型可见即日志可重建。** 新增给模型的上下文、记忆或压缩结果必须有稳定来源，并能从 session log 或明确的持久化事件重建。
- **配置显式且可验证。** token 预算、阈值、模型、存储位置、保留策略等部署选择进入带默认值和校验的 `Config`；错误配置尽早失败。
- **失败可观测。** 不静默跳过缺失依赖、损坏状态或无效候选；错误要携带插件、阶段和可操作原因，同时避免记录机密内容。
- **确定性核心，概率性边缘。** 选择、版本、回滚、数据切分和指标计算应确定；LLM 只用于必须依赖语义判断的生成或评分环节。
- **依赖公开契约。** 不导入 DSH 未导出的源码路径，不复制上游内部实现；确实缺少扩展点时，先形成最小上游提案。

## 自优化的安全边界

“自优化”表示生成候选、离线或影子评测、人工或策略审批、版本晋级和可回滚部署，不表示运行中的插件可以直接覆盖稳定实现。

- 稳定版本与候选版本分离；每个候选记录父版本、生成输入、配置、代码或内容摘要和创建时间。
- 训练、调参与评测数据分离；禁止根据保留评测集结果反复修改候选。
- 晋级必须同时比较质量、成功率、成本、延迟、安全和稳定性，遵循 [`docs/EVALUATION.md`](docs/EVALUATION.md)。
- 一次实验、单一提示或单一模型裁判不能证明改进。报告样本量、方差、失败案例和已知限制。
- 任何自动晋级都要有最大影响范围、停止条件、回滚点和审计日志；高风险变更默认需要人工批准。
- 不把密钥、完整用户私密内容或未经处理的工具输出写入候选、记忆、fixture 或实验报告。

## 各能力的最低要求

### 记忆整理与演化

- 区分工作记忆、情节记忆、语义记忆和程序性记忆，不把聊天全文直接当长期记忆。
- 每条记忆保存来源、主体、作用域、时间、置信度和版本；支持更正、失效、删除和冲突表示。
- 检索必须受当前任务、权限与工作区作用域约束。巩固和遗忘策略需要可解释指标及回放测试。

### Skill 演化

- 原始 skill 和已发布版本不可被候选生成过程原地修改。
- 优化输入必须包含真实失败证据；候选要通过触发准确性、任务成功率、工具安全性、token 成本和负迁移评测。
- 防止 skill 通过读取保留答案、修改评分器或放宽断言来提高分数。

### 上下文压缩

- 优先保留当前目标、用户约束、已确认事实、未完成事项、关键工具结果、文件位置和错误证据。
- 保持工具调用与结果配对、消息顺序和来源归属；不把推断写成事实。
- 压缩必须可重复触发且近似幂等；连续压缩不能造成无界语义漂移。
- 同时测量任务保真、关键信息召回、token 节省、延迟和后续任务成功率。

### Harness 自评测与实验优化

- 评测器不得由被测候选修改；数据集、评分器和基线需要版本固定。
- 优先使用外部世界状态验证结果，例如重新读取文件或运行真实命令，而不是相信 agent 自述。
- keyless 可重复测试负责回归，真实模型测试负责验证端到端有效性；两者不能互相替代。

## 实施流程

1. 写清问题、基线、假设、目标指标和不允许退化的指标。
2. 找到最小 DSH 扩展点，列出持久化、生命周期、权限和失败语义。
3. 先建立能复现旧问题的测试或评测案例，再实现最小改动。
4. 增加单元测试、真实 Loader 组合测试，并按用户可见程度增加端到端或 snapshot 证据。
5. 运行目标插件的 `pnpm run check`；跨插件变更再运行顶层 `scripts/check-all.ps1`。
6. 同步 README、配置说明、迁移或回滚方法；非平凡策略变更附实验报告。

完成汇报要列出实际运行的检查、未运行的真实模型评测以及尚未验证的风险，不用“应该可用”代替证据。

## TypeScript 与 Cordis 约定

- ESM、TypeScript strict 模式；包内相对导入使用 `.ts`，发布入口提供 ESM 和声明文件。
- `@deepseek-ai/cordis` 同时是 peer dependency 和 dev dependency。
- 导出稳定的 `name`、`Config` 和 `apply`；只有需要提供服务时才使用 `Service` 子类。
- 公共导出和非直观行为使用简洁 JSDoc；避免 `any`，在文件、JSON、模型、工具、进程或网络入口验证外部数据。
- waterfall listener 必须显式调用 `next()` 以继续链路；短路必须是有测试的设计决定。
- 不硬编码会随部署变化的模型、阈值、路径、预算和保留期限。

## Submodule 操作

```powershell
git submodule status --recursive
git submodule update --init --recursive
git submodule update --remote <plugin>
```

`update --remote` 会改变本地插件 checkout，执行前检查工作区；不要在插件有未提交修改时运行。顶层提交只固定已存在于插件仓库中的 commit。若插件托管位置不在顶层仓库的同一组织，更新 `.gitmodules` 中的 URL 并执行一次同步。
