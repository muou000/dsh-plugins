# AGENTS.md

## 工作区范围

`dsh-plugins` 是六个 DSH 仓库外插件的组合仓库。顶层仓库只有文档、检查脚本和 submodule 指针；每个一级插件目录都是独立 Git 仓库。

本文件适用于整个工作区。插件目录内如果有自己的 `AGENTS.md`，应同时遵守，并以更具体的要求为准。

## 开始工作前

1. 阅读当前任务要求、目标插件的 `AGENTS.md` 和 README。
2. 阅读 `D:\deepseek-harness\docs\architecture.md`。涉及事件监听、并发任务、子进程、文件观察或卸载时，再阅读 `docs\defensive-patterns.md`。
3. 分别检查顶层仓库和目标插件的 `git status --short --branch`。
4. 从 `package.json`、`cordis.patch.yml`、`src/` 和 `tests/` 核对实际行为，不要只相信旧文档或实验报告。

已有修改属于用户。保留与当前任务无关的改动；如果同一文件已有修改，先理解后再编辑。

## 仓库边界

- 插件功能、测试和插件自己的文档在对应插件仓库中修改。
- 顶层仓库只修改共同文档、组合脚本和 submodule 指针，不复制插件源码。
- 跨插件修改要分别检查和提交，说明启用顺序和兼容关系。
- 除非用户明确要求，不创建远程仓库、不推送、不发布、不改写历史，也不批量更新全部 submodule。
- 除非任务明确要求，不修改 `D:\deepseek-harness`。如果上游没有所需公开接口，先说明缺口和最小改动建议。

## 实现要求

- 只使用 DSH 公开导出的服务、事件和注册接口，不导入未导出的源码路径。
- 事件监听、服务注册、定时器、文件观察、子进程和后台任务必须能在插件卸载时停止并释放。
- 会进入模型上下文的内容必须能从 session log 或有版本的持久化记录重新得到，不能只存在于内存。
- 部署时会变化的模型、路径、阈值、预算和保留时间必须是经过校验的配置，不能写死在实现中。
- 文件、JSON、模型响应、命令输出和网络数据都按不可信输入处理。错误要说明插件名、出错阶段和可采取的处理办法，但不能泄露密钥或完整私有内容。
- 选择规则、版本、数据切分、回滚和指标计算应保持可重复。只有确实需要理解文本含义时才调用模型。

## TypeScript 和 Cordis 约定

- 使用 ESM 和 TypeScript strict 模式；包内相对导入带 `.ts` 后缀。
- 对外入口导出稳定的 `name`、`Config` 和 `apply`。只有插件确实提供共享服务时才创建 `Service` 子类。
- `@deepseek-ai/cordis` 同时放在 `peerDependencies` 和 `devDependencies`。
- 避免 `any`。对外类型和不直观的行为写简短 JSDoc。
- waterfall 事件监听器要显式调用 `next()` 才会继续后续处理；有意停止处理时必须有测试。
- 所有注册都绑定插件生命周期，初始化失败也要清理已经创建的资源。

## 测试和验收

先用测试复现要解决的问题，再做最小修改。根据改动风险覆盖以下内容：

- 配置默认值、边界值和错误输入。
- 正常加载、主要功能、卸载和重复清理。
- 通过真实 Cordis Loader 加载仓库中的 `cordis.patch.yml`。
- 持久化、并发、取消、重试和损坏输入等与改动有关的风险。
- 从打包产物或 Git 安装后的基本运行检查。

在目标插件中运行：

```powershell
corepack pnpm run check
```

跨插件修改再从顶层运行：

```powershell
.\scripts\check-all.ps1
```

`pnpm run check` 只代表类型检查、普通测试和构建通过。真实模型试验、外部 CodeGraph、打包测试或长时间基准必须单独运行并单独报告。

## 文档要求

README 必须以当前源码和可重复检查为准，并让第一次接触项目的人能够回答以下问题：

- 这个插件现在能做什么，明确不能做什么。
- 需要哪些 DSH、Node.js、pnpm 或外部程序。
- 如何安装、启用、配置、验证、停用和回滚。
- 数据写到哪里，如何备份、导出或删除。
- 哪些结论来自普通测试，哪些仍缺真实模型或生产环境验证。

使用简明中文。不可避免的 DSH 名称在第一次出现时解释其作用。不要把计划写成现有功能，不要把某台机器的启用状态写成项目默认状态，也不要用“智能闭环”“能力接缝”“自动进化”等宽泛词代替具体行为。

实验报告是带日期的证据快照。README 引用旧报告时必须说明日期，不能把旧性能数字写成当前保证。

## 涉及自动改进的额外限制

能够生成新提示、skill、记忆或配置的功能，只能先产生候选，不能直接覆盖正在使用的稳定版本。候选需要记录来源、父版本、输入摘要、配置、创建时间和内容摘要，并经过独立测试或人工审核后才能启用。

开发数据、调参数据和最终测试数据要分开。候选不得读取最终测试答案或修改评分程序。任何自动启用都必须有限定范围、停止条件、可恢复版本和审计记录；高风险变更默认需要人工确认。

## 完成汇报

完成时列出：

1. 修改了哪些仓库和文件。
2. 实际运行了哪些检查及结果。
3. 哪些真实模型、外部程序或平台测试没有运行。
4. 仍然存在的已知限制或兼容风险。

不要用“应该可用”代替检查结果。

## Submodule 常用命令

```powershell
git submodule status --recursive
git submodule update --init --recursive
git submodule update --remote <plugin>
```

`update --remote` 会改变插件 checkout。执行前必须确认目标插件没有未提交修改。顶层只能固定已经存在于插件仓库中的提交；如果修改 `.gitmodules` 中的远程地址，随后运行 `git submodule sync --recursive`。
