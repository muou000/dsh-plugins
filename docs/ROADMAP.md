# dsh-plugins 路线图

路线图按依赖关系组织，而不是按想法出现的顺序组织。评测和可观测能力要早于自动自优化，否则系统只能产生变化，不能可靠判断变化是否更好。

## Phase 0：工作区与插件基线

- 将顶层仓库作为 submodule 组合与共同规范的来源。
- 固化插件模板、生命周期、配置、测试和发布要求。
- 为每个插件建立真实 Loader 组合测试与干净安装 smoke test。
- 记录 DSH 兼容版本，并提供批量检查入口。

完成条件：从全新 clone 能初始化全部 submodule，并用一个命令执行各插件检查。

## Phase 1：评测底座与上下文压缩

### `dsh-eval`

- 定义任务案例、回放 fixture、评分器、运行 manifest 和差异报告。
- 支持基线/候选配对运行、重复采样、成本和延迟统计。
- 从外部世界状态验证代码任务结果，提供 keyless 与真实模型两条通道。
- 固定评测器版本，防止候选修改数据或评分逻辑。

### `improved-compact`

- 建立 DSH 当前压缩策略的可复现基线。
- 分离区域选择、token 预算、摘要生成、验证和落盘事件。
- 保留目标、约束、决定、未完成事项、来源及工具调用/结果配对。
- 测试重复压缩、跨轮恢复、模型失败、取消和摘要漂移。

完成条件：压缩候选在保留数据上证明任务保真不退化，同时报告 token、延迟和成本变化。

## Phase 2：长期记忆

### `dsh-memory`

- 定义工作、情节、语义和程序性记忆的数据模型与作用域。
- 建立带来源的提取、去重、冲突、更正、巩固、遗忘和删除流程。
- 将检索结果作为可追踪的模型输入写入 session log。
- 建立跨项目隔离、提示注入、错误记忆和过期记忆评测。

完成条件：在长周期代码任务中提高后续成功率或减少重复探索，且无跨作用域泄漏和不可更正的错误记忆。

## Phase 3：Skill 演化

### `dsh-skill-evolution`

- 从真实失败轨迹生成有父版本的候选，而不修改已发布 skill。
- 评测触发准确性、任务成功、负迁移、工具安全和 token 成本。
- 支持候选 diff、人工评审、灰度、晋级、降级和回滚。
- 检测针对评分器投机、保留集污染和指令无限膨胀。

完成条件：候选在多任务族和未见案例上达到预注册门槛，并能一键恢复稳定版本。

## Phase 4：受控闭环优化

- 用 `dsh-eval` 统一比较 memory、skill 和 compaction 的组合效果。
- 使用影子运行积累候选证据，限制自动修改频率和影响范围。
- 只有低风险、可回滚且达到统计门槛的候选允许策略自动晋级。
- 对数据漂移、模型版本变化、成本异常和安全事件设置熔断。

完成条件：闭环拥有完整审计链、预算上限、人工接管、熔断和回滚演练记录。

## 跨阶段原则

- 每个 phase 都能独立交付有用能力，不能依赖未来的“智能优化”掩盖当前缺陷。
- 优先建立数据契约、可回放日志和评测，再引入新的模型调用。
- 同时优化质量、成本、延迟和安全；不以单一排行榜替代产品判断。
- 对 DSH 上游缺失能力提交最小扩展点提案，插件不依赖未公开内部实现。
